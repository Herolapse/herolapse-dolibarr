# Merge changes from new release

0. Make sure the repo is clean and you're on `production` branch
1. Rebase against new release tag (for example 23.0.3): `git rebase 23.0.3`
2. Test if everything works locally
3. Push
